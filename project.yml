name: TensorLabsVoice
options:
  minimumXcodeGenVersion: 2.38.0
  createIntermediateGroups: true
configs:
  Debug: debug
  Release: release
packages:
  WhisperKit:
    url: https://github.com/argmaxinc/WhisperKit.git
    from: 0.9.0
  SpeechSwift:
    url: https://github.com/soniqo/speech-swift
    branch: main
targets:
  TensorLabsVoice:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: TensorLabsVoice
    info:
      path: XcodeSupport/Info.plist
    entitlements:
      path: XcodeSupport/TensorLabsVoice.entitlements
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.tensorlabs.voice
        PRODUCT_NAME: TensorLabsVoice
        SWIFT_VERSION: 6.0
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.productivity
        INFOPLIST_KEY_LSUIElement: YES
        INFOPLIST_KEY_NSMicrophoneUsageDescription: TensorLabs Voice needs microphone access to capture your speech.
        INFOPLIST_KEY_NSSpeechRecognitionUsageDescription: TensorLabs Voice needs speech recognition access to transcribe your speech when the Apple fallback engine is used.
    dependencies:
      - package: WhisperKit
        product: WhisperKit
      - package: SpeechSwift
        product: Qwen3ASR
      - package: SpeechSwift
        product: ParakeetASR
    postBuildScripts: []
schemes:
  TensorLabsVoice:
    build:
      targets:
        TensorLabsVoice: all
    run:
      config: Debug
    archive:
      config: Release
